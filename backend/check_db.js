const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('Config:', await prisma.hackathonConfig.findUnique({where:{id:1}})); console.log('Teams Count:', await prisma.team.count()); console.log('Submissions Count:', await prisma.submission.count()); const sample = await prisma.submission.findFirst(); console.log('Sample Submission:', sample ? {id:sample.id, hasContent: git push origin main --verbosesample.content} : 'None'); } main().catch(console.error).finally(() => prisma.$disconnect());
