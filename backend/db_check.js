const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.team.count(); console.log('Total teams:', count); const teams = await prisma.team.findMany({ take: 5 }); console.log('Sample teams:', JSON.stringify(teams, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
